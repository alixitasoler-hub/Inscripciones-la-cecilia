import React from 'react';

// Componente simple de prueba
const AdminPanel = () => {
  return (
    <div style={{ padding: '20px' }}>
      <h1>¡El Panel de Administración está cargando!</h1>
      <p>Si ves esto, el problema no es la ruta principal, sino los componentes internos.</p>
    </div>
  );
};

export default AdminPanel;
